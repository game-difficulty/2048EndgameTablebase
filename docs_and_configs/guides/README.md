# Guide Sources

Source documents in `sources/` are offline inputs for `tools/guide_image_parser/`.
They are not copied into `frontend/public` and are not included in production
deployment archives. Browser-ready text, media, and board metadata live under
`frontend/public/guides/`.
